= form_for @post do |f|
  - if @post.errors.any?
    #error_explanation
      %h2= "#{pluralize(@post.errors.count, "error")} prohibited this post from being saved:"
      %ul
        - @post.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :content
    = f.text_area :content
  .field
    = f.label :post_img
    = f.file_field :post_img, :value => 'covers/cover1.jpeg'
  .field
    = f.label :embed_url
    = f.text_field :embed_url
  .field
    = f.label :tag_list
    = f.text_field :tag_list, { :placeholder => "Tags separated by commas" }
  .actions
    = f.submit 'Save'
